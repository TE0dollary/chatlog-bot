# GoReleaser v2 配置
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: chatlog-builds
    binary: chatlog
    ldflags:
      - -s -w -X github.com/TE0dollary/chatlog-bot/pkg/version.Version={{.Version}}
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    overrides:
      - goos: darwin
        goarch: amd64
        env:
          - CGO_ENABLED=1
          - CC=o64-clang
          - CXX=o64-clang++
      - goos: darwin
        goarch: arm64
        env:
          - CGO_ENABLED=1
          - CC=oa64-clang
          - CXX=oa64-clang++
      - goos: linux
        goarch: amd64
        env:
          - CGO_ENABLED=1
          - CC=x86_64-linux-gnu-gcc
          - CXX=x86_64-linux-gnu-g++
      - goos: linux
        goarch: arm64
        env:
          - CGO_ENABLED=1
          - CC=aarch64-linux-gnu-gcc
          - CXX=aarch64-linux-gnu-g++

upx:
  - enabled: true
    goos: [darwin, linux]
    goarch: [amd64, arm64]
    compress: best
    lzma: true

archives:
  - id: default
    formats: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - LICENSE
      - README.md

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

# 配置 GitHub Release
release:
  draft: true
  prerelease: auto
  mode: replace

# Docker 镜像配置
# FIXME: 等待 goreleaser 2.12 更新后使用 dockers_v2
dockers:
  - image_templates:
      - "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}-amd64"
      - "te0dollary/chatlog-bot:{{ .Tag }}-amd64"
    dockerfile: Dockerfile
    extra_files:
      - script/docker-entrypoint.sh
    use: buildx
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.description=chat log tool, easily use your own chat data."
      - "--label=org.opencontainers.image.url=https://github.com/TE0dollary/chatlog-bot"
      - "--label=org.opencontainers.image.source=https://github.com/TE0dollary/chatlog-bot"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.licenses=Apache-2.0"

  - image_templates:
      - "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}-arm64"
      - "te0dollary/chatlog-bot:{{ .Tag }}-arm64"
    dockerfile: Dockerfile
    extra_files:
      - script/docker-entrypoint.sh
    use: buildx
    goos: linux
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.description=chat log tool, easily use your own chat data."
      - "--label=org.opencontainers.image.url=https://github.com/TE0dollary/chatlog-bot"
      - "--label=org.opencontainers.image.source=https://github.com/TE0dollary/chatlog-bot"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.licenses=Apache-2.0"

# Docker manifest 配置用于多架构镜像
docker_manifests:
  # GitHub Container Registry manifests
  - name_template: "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}"
    image_templates:
      - "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}-amd64"
      - "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}-arm64"

  # Docker Hub manifests
  - name_template: "te0dollary/chatlog-bot:{{ .Tag }}"
    image_templates:
      - "te0dollary/chatlog-bot:{{ .Tag }}-amd64"
      - "te0dollary/chatlog-bot:{{ .Tag }}-arm64"

  # GitHub Container Registry latest
  - name_template: "{{ if not .Prerelease }}ghcr.io/te0dollary/chatlog-bot:latest{{ end }}"
    image_templates:
      - "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}-amd64"
      - "ghcr.io/te0dollary/chatlog-bot:{{ .Tag }}-arm64"

  # Docker Hub latest
  - name_template: "{{ if not .Prerelease }}te0dollary/chatlog-bot:latest{{ end }}"
    image_templates:
      - "te0dollary/chatlog-bot:{{ .Tag }}-amd64"
      - "te0dollary/chatlog-bot:{{ .Tag }}-arm64"